supabase>=2.0
python-dotenv>=1.0
boto3>=1.34
ultralytics>=8.0

# Face identification.
# Install face_recognition with --no-deps so it doesn't try to build dlib
# from source (which needs CMake + MSVC and has no Windows wheels).
# dlib-bin is a community-built prebuilt wheel that satisfies the runtime
# requirement.
#
# Run from station/ once:
#   pip install -r requirements.txt
#   pip install dlib-bin "setuptools<81"
#   pip install --no-deps face_recognition face_recognition_models
dlib-bin>=20.0
setuptools<81
numpy>=1.26
# face_recognition + face_recognition_models intentionally NOT listed here.
# Install them with --no-deps from the commands above.
