# Node / Next.js
node_modules/
.next/
out/
*.log
tsconfig.tsbuildinfo
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (NEVER commit these — they hold secrets)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Python (for future station/ and ml/ folders)
__pycache__/
*.pyc
.venv/
venv/

# Local test photos (don't commit)
station/sample-tray.jpg
*.egg-info/

# ML model artifacts
*.pt
*.onnx
*.h5
runs/
# ...except the trained weights we ship with the station code
!station/best.pt

# ML training data (huge — keep raw zips and unpacked datasets out of git)
ml/dataset/
ml/*.zip
ml/runs/
ml/.venv/

# Caveman / agent skill installer artifacts (per-machine, not project code)
.agents/
.claude/
skills-lock.json
